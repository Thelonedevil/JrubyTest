<% content_for :title do %><%='Home'%><%end%>
<div>
    <% @channels.each do |channel| %>
        <a href="#" data-dropdown="drop-<%= channel %>" class="button"><%= channel %></a>

        <div id="drop-<%= channel %>" class="f-dropdown content text-center" data-dropdown-content>
            <a href="dashboard/channel/<%= channel %>/get/all">All</a>
            <br>
            <a href="dashboard/channel/<%= channel %>/get/own">Broadcaster</a>/<a href="dashboard/channel/<%= channel %>/get/own/all">below</a>
            <br>
            <a href="dashboard/channel/<%= channel %>/get/smd">Super-Moderator</a>/<a href="dashboard/channel/<%= channel %>/get/smd/all">below</a>
            <br>
            <a href="dashboard/channel/<%= channel %>/get/mod">Moderator</a>/<a href="dashboard/channel/<%= channel %>/get/mod/all">below</a>
            <br>
            <a href="dashboard/channel/<%= channel %>/get/reg">Regular</a>/<a href="dashboard/channel/<%= channel %>/get/reg/all">below</a>
            <br>
            <a href="dashboard/channel/<%= channel %>/get/sub">Subscribers</a>/<a href="dashboard/channel/<%= channel %>/get/sub/all">below</a>
            <br>
            <a href="dashboard/channel/<%= channel %>/get/usr">Users</a> <br>

        </div>
    <% end %>
</div>